name: Python CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install uv
      run: |
        curl -LsSf https://astral.sh/uv/install.sh | sh
        echo "$HOME/.cargo/bin" >> $GITHUB_PATH

    - name: Install Dependencies
      run: |
        uv venv
        source .venv/bin/activate
        uv pip install -e .[dev] -e dia
        # Install optional dependencies (may have conflicts, but that's OK)
        uv pip install mem0ai || echo "mem0ai installation failed (optional)"

    - name: Lint with ruff
      run: |
        source .venv/bin/activate
        ruff check .

    - name: Test with pytest
      run: |
        source .venv/bin/activate
        pytest